# wysiwyg-ckeditor-tinymce-sample

install bower package using npm 
```$ npm install -g bower```

install dependencies listed in bower.json
```$ bower install```

install a package and add it to bower.json
```$ bower install <package> --save```
